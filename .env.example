# PostgreSQL Database Configuration for TraceX
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tracex
DB_USER=tracex_user
DB_PASSWORD=tracex_secure_password

# Superuser credentials (used only during initial database/user provisioning)
PG_SUPERUSER=postgres
PG_SUPERPASSWORD=your_postgres_admin_password
